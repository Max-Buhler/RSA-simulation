# RSA Simulation

The simulation is a chat, where two clients can send each other messages. Each client has its own website
- https://rsa-client-1.onrender.com/
- https://rsa-client-2.onrender.com/

Each client has two generate their own keys, via two (preferable large) prime numbers. The other client is then able two use the public keys to encrypt a one letter message
